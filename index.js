/**
 * @format
 */

import { AppRegistry } from 'react-native';
import CoreConnect from './CoreConnect';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => CoreConnect);
